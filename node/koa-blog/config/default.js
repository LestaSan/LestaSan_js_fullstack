const config = {
    port: 3000,
    database: {
        DATABASE: 'nodesql',
        USERNAME: 'root',
        PASSWORD: '12345678',
        POST: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;